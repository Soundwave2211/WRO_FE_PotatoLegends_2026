The simulator is complete. In future updates, the vision model and impleentation onto physical bot shall be done.

To run sim, type this into terminal:
pip3 install -r requirements.txt
python3 simulator/viewer.py